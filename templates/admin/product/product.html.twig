{% extends 'base.html.twig' %}

{% block title %}Gestion des produits{% endblock %}
{% block body %}
<div class="container mt-5">
    <a href="{{ path('admin_product_new') }}" class="btn btn-primary">Ajouter</a>
    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Price</th>
                  <th>Image</th>
              
                <th>Quantity</th>
              
                <th>Coupe</th>
              
                <th>Type</th>
                <th>Action</th>
                
           
               
            </tr>
        </thead>
        <tbody>
            {% for product in products %}
                <tr>
                    <td>{{ product.id }}</td>
                  <td>{{ product.price }}€/m3</td>
                 <td><img src="{{ asset('/uploads/images/' ~ product.image) }}" alt="Image" width="100" /></td>
                    <td>{{ product.quantity }}</td>
                    <td>{{ product.coupe.name }}</td>
                  
                    <td>{{ product.type.name }}</td>

    

                 
                  
                            
                   
                  
             
                  
                   
                     
                    <td>
                        <a href="{{ path('admin_product_update', {id: product.id}) }}"  class="text-warning">Modifier</a>
                        <a href="{{ path('admin_product_delete', {id: product.id}) }}"  class="text-danger">Supprimer</a>
                    </td>
                    
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}